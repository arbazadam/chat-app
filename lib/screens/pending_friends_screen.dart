import 'package:flutter/material.dart';

class PendingFriends extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Pending friends')),
    );
  }
}
